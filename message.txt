my-nextjs-app/
│── app/
│   ├── layout.tsx                # Root layout (global <html>, <body>, providers, navbar if needed)
│   ├── page.tsx                  # Home page (public landing page)
│
│   ├── (auth)/                   # Group for auth routes with their own layout
│   │   ├── layout.tsx            # Auth layout (centered form, no sidebar/header)
│   │   ├── signin/
│   │   │   └── page.tsx          # Sign in page
│   │   ├── signup/
│   │   │   └── page.tsx          # Sign up page
│   │   └── forgot-password/
│   │       └── page.tsx          # Optional: password reset
│
│   ├── (dashboard)/              # Group for dashboard (protected area)
│   │   ├── layout.tsx            # Dashboard layout (sidebar, topbar, etc.)
│   │   ├── page.tsx              # Dashboard index (/dashboard)
│   │   ├── profile/
│   │   │   └── page.tsx          # Profile page
│   │   ├── settings/
│   │   │   └── page.tsx          # Settings page
│   │   └── products/
│   │       ├── page.tsx          # Products list
│   │       └── [id]/
│   │           └── page.tsx      # Product detail (dynamic route)
│
│   ├── about/
│   │   └── page.tsx              # Public "About" page
│   └── contact/
│       └── page.tsx              # Public "Contact" page
│
├── components/                   # Shared React components
│   ├── ui/                       # Buttons, inputs, cards
│   ├── auth/                     # Auth-specific components (form, input groups)
│   └── dashboard/                # Dashboard widgets, sidebar, navbar
│
├── lib/                          # Helpers (API calls, db client, auth utils)
│   ├── auth.ts                   # Token/JWT logic
│   ├── db.ts                     # Database client (prisma/sqlite/whatever)
│   └── fetcher.ts                # Fetch wrapper
│
├── middleware.ts                 # Protect routes (check JWT/session before dashboard)
├── public/                       # Static files (images, favicon, etc.)
├── styles/                       # Global styles
├── package.json
└── tsconfig.json
